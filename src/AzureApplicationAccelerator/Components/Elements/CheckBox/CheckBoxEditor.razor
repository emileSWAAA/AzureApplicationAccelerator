@using AzureApplicationAccelerator.Elements.Models.Elements

<FluentAccordionItem Heading="Constraints">
    <RequiredConstraint Required="@Element.Constraints?.Required" />
    <ValidationMessageConstraint ValidationMessage="@Element.Constraints?.ValidationMessage" />
</FluentAccordionItem>

@code {
    [Parameter] public CheckBoxElement Element { get; set; } = default!;
}